server:
    port: 8082

spring:
  application:
    name: SpringBoot

  datasource:
    url: jdbc:mysql//localhost:3306/spring_boot ?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

